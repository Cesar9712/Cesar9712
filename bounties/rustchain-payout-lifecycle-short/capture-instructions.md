# 9:16 Capture Instructions

Target: 1080×1920, 30 fps, 45–55 seconds.

## Shot 1 — 0:00–0:05
Large text on a dark background:

`BOUNTY ACCEPTED ≠ PAYMENT FINAL`

Use a subtle typewriter animation. No token-price or dollar graphics.

## Shot 2 — 0:05–0:18
Screen-record the public GitHub issue `Scottcjn/rustchain-bounties#16601`. Crop to the “How To Claim” section. Highlight only:

`accepted → paid (two-phase: queued, confirms ~24h; we post the pending_id + tx)`

Overlay a simple three-box flow:

`ACCEPTED` → `QUEUED` → `CONFIRMED`

## Shot 3 — 0:18–0:33
Show the same issue text again, zoomed on `confirms ~24h`. Add a clock icon and the caption:

`PENDING ≠ FINAL`

Do not claim an exact SLA. Keep the tilde visible when showing `~24h`.

## Shot 4 — 0:33–0:45
Show the words `pending_id` and `tx` from the public issue. Animate a checklist:

- accepted
- pending ID
- transaction evidence

Only the last item gets a green “confirmed” badge.

## Shot 5 — 0:45–0:54
Full-screen checklist:

`SUBMITTED`
`ACCEPTED`
`CONFIRMED PAID`

Underline the last state while narration says to track them separately.

## End card
`RustChain bounty workflow`
`github.com/Scottcjn/rustchain-bounties`

## Editing guardrails
- Burn subtitles into every spoken section.
- Use only public GitHub screenshots or original motion graphics.
- No wallet keys, private addresses, personal email, token-price claims, or profit promises.
- No claim that all RustChain bounties use an identical schedule; this Short describes the public #16601 flow.
