# Vertical capture instructions

Canvas: 1080×1920, 9:16. Target runtime: 45–55 seconds.

1. **0–5s — Hook.** Large text: `HANDLE ≠ NATIVE RTC ADDRESS`. Under it, show two neutral cards: `@contributor` and `RTC…`.
2. **5–17s — Handle fallback.** Screen-record the public bounty-payout source/commentary describing handle fallback. Highlight only the relevant line; blur/no secrets because none are needed.
3. **17–31s — Native destination.** Show bounty #16601's current claim instruction requesting `RTC…` format and warning about bare handles. Zoom on the exact rule.
4. **31–43s — Mental model.** Simple two-row graphic: `Handle → who earned it` and `RTC address → where settlement goes`.
5. **43–51s — Safety.** Full-screen text: `Receiving address: public` / `Seed & private key: NEVER SHARE`.
6. **51–55s — End card.** `Read the live bounty spec before every claim.`

Use only public GitHub pages and original typography/shapes. No wallet balances, price claims, or private credentials.