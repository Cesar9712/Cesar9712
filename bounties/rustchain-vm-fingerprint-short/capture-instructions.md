# 9:16 Capture Instructions

Target canvas: 1080×1920, 30 fps. Keep every code/source crop large enough to read on a phone.

## Shot 1 — 0:00–0:04
Black terminal-style background. Large centered text: `CAN A VM PRETEND TO BE VINTAGE HARDWARE?` Add a quick 105%→100% scale-in. No stock footage required.

## Shot 2 — 0:04–0:16
Screen-record the pinned RustChain Proof-of-Antiquity specification at `specs/RIP_POA_SPEC_v1.0.md`. Crop tightly around the anti-emulation table so `vm_indicators` and “Any single indicator is sufficient to flag virtualization” are visible. Highlight only those lines.

## Shot 3 — 0:16–0:31
Continue on the same pinned specification. Scroll to the enforcement paragraph and highlight `fingerprint_passed = 0` plus enrollment weight `0.000000001`. Overlay a simple two-state diagram:

`VM indicator detected` → `fingerprint failed` → `weight 0.000000001`

Do not add percentages or dollar values.

## Shot 4 — 0:31–0:43
Split screen. Left: a generic terminal JSON card reading `arch: "486"` with a small caption `self-reported`. Right: the specification’s anti-emulation evidence lines with caption `behavior checked`. Add a large `≠` between them. This is an explanatory graphic, not a claim that the literal sample JSON is accepted by the live node.

## Shot 5 — 0:43–0:52
Record a clean terminal typing:

```bash
python3 -m pip install clawrtc
```

Then show `github.com/Scottcjn/Rustchain` as the final destination. Do not enter any wallet keys or credentials in the recording.

## End card — 0:52–0:56
Text only:

`RustChain`
`Proof of Antiquity`
`Open source • inspect the checks yourself`

## Editing notes
- Captions burned in throughout.
- Fast cuts, no transition longer than 4 frames.
- No background music required; if added, use original/CC0 material only.
- Avoid “unhackable”, “guaranteed”, “secure forever”, price claims, or profit language.
